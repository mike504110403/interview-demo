══════════════════════════════════════════════════
  ✅ Pritunl (OpenVPN) 登入資訊
══════════════════════════════════════════════════

🌐 GUI
   https://localhost:9700

🔑 Setup Key
   19b3039dc40f48ac84a1ae7d3c629c86

👤 登入
   Username: pritunl
   Password: 執行以下命令獲取
   
   cd /Users/mike/Documents/self/interview-demo/vpn-server/docker/pritunl
   docker-compose exec pritunl pritunl default-password

📝 問題說明
   Setup Key 可以在每次啟動時透過以下命令獲取：
   docker-compose exec pritunl pritunl setup-key
   
   Setup Key 無法在 docker-compose.yml 中預設，
   每次清空 volume 會重新生成新的 key。

🌐 DNS 設定 (test.dev → 119.8.53.204)
   請在 Pritunl GUI 中手動配置：
   1. 創建 Server
   2. 進入 Server Settings
   3. 在 DNS Servers 或 Routes 中添加自訂域名

✅ 運行狀態
   MongoDB 4.4: 正常 (ARM64 原生)
   Pritunl: 正常 (x86_64 emulated)
   DNS Container: 已移除（掛載問題）

💡 關於 Setup Key 在 docker-compose 中設定
   Setup Key 是在首次初始化時生成的認證金鑰，
   用於首次登入並綁定管理員帳戶。
   
   無法透過環境變量預設，因為它是：
   1. 動態生成的安全金鑰
   2. 只在首次設定時使用一次
   3. 設定完成後會失效
   
   如需固定部署：
   - 保留 pritunl-data volume (不要 -v)
   - 或使用 Pritunl API 自動化設定
   - 或手動備份/還原配置文件

═══════════════════════════════════════════
  ✅ DNS 伺服器設定完成
═══════════════════════════════════════════

🌐 服務架構
   ┌─────────────────┐
   │  VPN 客戶端      │
   └────────┬────────┘
            │ 連接
   ┌────────▼────────┐
   │  Pritunl Server │
   │  DNS: 172.25.0.2│ ← 指向 dnsmasq
   └────────┬────────┘
            │ DNS 查詢
   ┌────────▼────────┐
   │  dnsmasq DNS    │
   │  172.25.0.2     │
   │  Port: 53       │
   └─────────────────┘

📋 DNS 配置
   ✅ test.dev → 119.8.53.204
   
   其他內部域名：
   - gitlab.internal → 10.0.1.10
   - dev.internal → 10.0.2.10
   - staging.internal → 10.0.3.10
   - docs.internal → 10.0.4.10
   - grafana.internal → 10.0.5.10

🔧 下一步：在 Pritunl GUI 中設定 DNS

1. 進入 Server Settings
2. 將 DNS Server 從 `8.8.8.8` 改為 `172.25.0.2`
3. 點擊 Save
4. 啟動 Server

🧪 測試方法

當 VPN 客戶端連接後：
```bash
# 測試 DNS 解析
nslookup test.dev
# 應該返回: 119.8.53.204

# 測試連接
ping test.dev
curl http://test.dev
```

📝 添加更多域名

編輯 dnsmasq.conf：
```bash
cd /Users/mike/Documents/self/interview-demo/vpn-server/docker/pritunl
nano dnsmasq.conf

# 添加新的域名
address=/your-domain.dev/1.2.3.4

# 重啟 dnsmasq
docker-compose restart dnsmasq
```

🌐 dnsmasq Web GUI
   http://localhost:5380
   （可查看 DNS 統計和日誌）

✅ 完成！

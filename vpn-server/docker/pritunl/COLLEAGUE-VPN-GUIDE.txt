═══════════════════════════════════════════════
  📝 同事連接 VPN 指南
═══════════════════════════════════════════════

## 📊 你的網絡信息

🖥️  **你的內網 IP**: 192.168.68.106
🔌  **VPN 端口**: 11009/udp
📡  **同事和你在同一個內網**: ✅

═══════════════════════════════════════════════

## ✅ 已完成的設置

1. ✅ VPN Server 綁定到所有網絡接口 (0.0.0.0)
   - 同事可以通過內網 IP 連接

2. ✅ Public Address 已設置為: 192.168.68.106
   - 下載的配置文件會自動使用這個 IP

3. ✅ 端口已映射並監聽
   - Docker 端口映射: 11009:11009/udp

═══════════════════════════════════════════════

## 📥 給同事的步驟

### 步驟 1：獲取配置文件

**你需要做的：**
1. 進入 Pritunl GUI: https://localhost:9700/#/users
2. 點擊用戶 "test" 右側的下載按鈕 (📥)
3. 配置文件會下載為 `.tar` 文件
4. 將這個文件發送給同事

### 步驟 2：同事安裝 OpenVPN 客戶端

**Mac:**
```bash
brew install --cask tunnelblick
# 或
brew install openvpn
```

**Windows:**
- 下載並安裝 OpenVPN GUI: https://openvpn.net/community-downloads/

**Linux:**
```bash
sudo apt install openvpn  # Ubuntu/Debian
sudo yum install openvpn  # CentOS/RHEL
```

### 步驟 3：導入配置並連接

**Tunnelblick (Mac):**
1. 解壓 `.tar` 文件
2. 雙擊 `.ovpn` 文件
3. Tunnelblick 會自動導入
4. 點擊 "Connect"

**OpenVPN GUI (Windows):**
1. 解壓 `.tar` 文件
2. 將 `.ovpn` 文件複製到:
   `C:\Program Files\OpenVPN\config\`
3. 右鍵點擊系統托盤的 OpenVPN 圖標
4. 選擇配置並點擊 "Connect"

**命令行 (所有系統):**
```bash
# 解壓配置文件
tar -xf *.tar

# 連接 (需要 root 權限)
sudo openvpn --config Test-VPN-Server_test.ovpn
```

═══════════════════════════════════════════════

## 🔍 確認配置文件內容

下載的 `.ovpn` 文件應該包含：
```
remote 192.168.68.106 11009 udp
```

**如果顯示的不是 192.168.68.106，可以手動編輯：**
1. 解壓 `.tar` 文件
2. 用文本編輯器打開 `.ovpn` 文件
3. 找到 `remote` 那一行
4. 改為: `remote 192.168.68.106 11009 udp`
5. 保存並導入到 OpenVPN 客戶端

═══════════════════════════════════════════════

## ✅ 連接成功後測試

同事連接成功後應該能：

1. **獲得 VPN IP**
   ```bash
   ifconfig | grep 192.168.219
   # 應該看到 192.168.219.x 的 IP
   ```

2. **Ping VPN Server**
   ```bash
   ping 192.168.219.1
   # 應該能 ping 通
   ```

3. **測試 DNS 解析**
   ```bash
   nslookup test.dev
   # 應該返回: 119.8.53.204
   ```

═══════════════════════════════════════════════

## ❌ 故障排除

### 連接失敗

1. **確認你的 Mac 防火牆允許端口 11009**
   ```bash
   # 查看防火牆狀態
   sudo /usr/libexec/ApplicationFirewall/socketfilterfw --getglobalstate
   
   # 如果啟用了防火牆，需要允許 Docker
   sudo /usr/libexec/ApplicationFirewall/socketfilterfw --add /Applications/Docker.app/Contents/MacOS/Docker
   sudo /usr/libexec/ApplicationFirewall/socketfilterfw --unblock /Applications/Docker.app/Contents/MacOS/Docker
   ```

2. **確認 VPN Server 運行中**
   ```bash
   cd /Users/mike/Documents/self/interview-demo/vpn-server/docker/pritunl
   docker-compose ps
   # pritunl 應該是 Up 狀態
   ```

3. **檢查端口監聽**
   ```bash
   netstat -an | grep 11009
   # 應該看到監聽在 0.0.0.0:11009 或 :::11009
   ```

4. **確認同事在同一個內網**
   - 同事的 IP 應該是 192.168.68.x
   - 可以互相 ping 通你的 IP

### DNS 不工作

如果連接成功但 DNS 解析不工作：
```bash
# 同事手動添加 hosts
sudo nano /etc/hosts  # Mac/Linux
# 添加: 119.8.53.204 test.dev

# Windows (以管理員打開記事本)
C:\Windows\System32\drivers\etc\hosts
# 添加: 119.8.53.204 test.dev
```

═══════════════════════════════════════════════

## 📝 總結

✅ **可以直接用從 Pritunl 下載的配置文件**
✅ **不需要修改任何 VPN Server 設置**
✅ **配置文件會自動包含內網 IP (192.168.68.106)**
✅ **同事導入配置後就能連接**

⚠️ **注意**: 如果你的電腦關機或 Docker 停止，VPN 就會斷線！

═══════════════════════════════════════════════

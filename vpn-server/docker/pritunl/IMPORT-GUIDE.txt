═══════════════════════════════════════════════
  📥 OpenVPN 配置文件導入指南
═══════════════════════════════════════════════

## ❓ .tar 可以直接導入嗎？

❌ **不行！.tar 是壓縮檔**
✅ **需要先解壓取出 .ovpn 文件**

═══════════════════════════════════════════════

## 📦 檔案結構

下載的檔案：
```
6943f29f529d6776acdca98a.tar
```

解壓後得到：
```
Test-VPN-Server_test.ovpn  ← 這個才能導入 OpenVPN
```

.ovpn 文件內容包含：
- remote 192.168.68.106 11009 udp
- 所有證書和密鑰
- DNS Server 設定
- 路由設定

═══════════════════════════════════════════════

## 🍎 Mac 用戶（Tunnelblick）

### 方法 1：最簡單 ⭐
```bash
# 1. 安裝 Tunnelblick
brew install --cask tunnelblick

# 2. 直接雙擊 .tar 文件
# Tunnelblick 會自動解壓並導入

# 3. 點擊 Tunnelblick 圖標 → Connect
```

### 方法 2：手動解壓
```bash
# 1. 解壓
tar -xf 6943f29f529d6776acdca98a.tar

# 2. 雙擊 Test-VPN-Server_test.ovpn
# Tunnelblick 會自動導入

# 3. 點擊 Connect
```

═══════════════════════════════════════════════

## 🪟 Windows 用戶（OpenVPN GUI）

### 步驟

1️⃣ **安裝 OpenVPN GUI**
   下載: https://openvpn.net/community-downloads/
   安裝時選擇完整安裝

2️⃣ **解壓 .tar 文件**
   - 右鍵 .tar 文件
   - 選擇 "解壓縮" 或用 7-Zip/WinRAR
   - 得到 Test-VPN-Server_test.ovpn

3️⃣ **複製 .ovpn 到 config 目錄**
   ```
   C:\Program Files\OpenVPN\config\
   ```
   或者
   ```
   C:\Users\你的用戶名\OpenVPN\config\
   ```

4️⃣ **連接**
   - 右鍵點擊系統托盤的 OpenVPN 圖標
   - 選擇 "Test-VPN-Server_test"
   - 點擊 "Connect"

═══════════════════════════════════════════════

## 🐧 Linux 用戶

### Ubuntu/Debian
```bash
# 1. 安裝 OpenVPN
sudo apt update
sudo apt install openvpn

# 2. 解壓配置文件
tar -xf 6943f29f529d6776acdca98a.tar

# 3. 連接（需要 root 權限）
sudo openvpn --config Test-VPN-Server_test.ovpn
```

### CentOS/RHEL
```bash
# 1. 安裝 OpenVPN
sudo yum install epel-release
sudo yum install openvpn

# 2. 解壓並連接
tar -xf 6943f29f529d6776acdca98a.tar
sudo openvpn --config Test-VPN-Server_test.ovpn
```

═══════════════════════════════════════════════

## 🔍 驗證連接成功

連接後測試：

### 1️⃣ 檢查 VPN IP
```bash
# Mac/Linux
ifconfig | grep 192.168.219

# Windows
ipconfig | findstr 192.168.219
```
應該看到 192.168.219.x 的 IP

### 2️⃣ Ping VPN Server
```bash
ping 192.168.219.1
```
應該能 ping 通

### 3️⃣ 測試 DNS 解析
```bash
nslookup test.dev
```
應該返回: 119.8.53.204

### 4️⃣ 測試訪問
```bash
curl http://test.dev
ping test.dev
```

═══════════════════════════════════════════════

## ⚠️ 常見問題

### Q1: .tar 雙擊後沒反應？
**A:** 
- Mac: 需要先安裝 Tunnelblick
- Windows: 需要用解壓軟件（7-Zip/WinRAR）

### Q2: 連接失敗？
**A:** 確認：
1. VPN Server 正在運行
   ```bash
   docker-compose ps
   ```
2. 兩台電腦在同一個內網（192.168.68.x）
3. 防火牆沒有阻擋 11009/udp 端口

### Q3: 連接成功但 DNS 不工作？
**A:** 手動添加 hosts：
```bash
# Mac/Linux
sudo nano /etc/hosts
# 添加: 119.8.53.204 test.dev

# Windows (管理員權限)
notepad C:\Windows\System32\drivers\etc\hosts
# 添加: 119.8.53.204 test.dev
```

### Q4: Windows 找不到 config 目錄？
**A:** 可能在以下位置：
- `C:\Program Files\OpenVPN\config\`
- `C:\Users\你的用戶名\OpenVPN\config\`
- 或者右鍵 OpenVPN 圖標 → "Import file"

═══════════════════════════════════════════════

## 📝 總結

✅ **.tar 需要先解壓**
✅ **解壓後得到 .ovpn 文件**
✅ **.ovpn 文件才能導入 OpenVPN**
✅ **Tunnelblick (Mac) 可以自動處理 .tar**
✅ **Windows 需要手動解壓**

📌 **最簡單的方式**：
- Mac 用戶用 Tunnelblick（可直接雙擊 .tar）
- Windows 用戶用 OpenVPN GUI（需先解壓）

═══════════════════════════════════════════════
